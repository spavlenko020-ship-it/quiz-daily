# Quiz Daily — Project Memory

## 🚨 AUDIT PROTOCOL — NON-NEGOTIABLE

Every response from the architect (Claude in the chat) MUST begin with one of exactly two lines in Ukrainian:

- **АУДИТ ПРОВЕДЕНО** — followed by the list of mistakes / risks / missed details found in this response before sending it. Even "no mistakes found" is only acceptable after a genuine self-check against the full conversation and the rules in this file.
- **АУДИТ НЕ ПРОВЕДЕНО** — followed by the reason (e.g. "message is a one-line confirmation, no technical content").

Audit means: before sending, re-read your own draft response and ask:
1. Am I writing from memory where I should have searched (web_search or conversation_search)?
2. Does this contradict anything I or CLAUDE.md said earlier?
3. Does my prompt for Claude Code leave any UI state ambiguous, any file untouched that should be touched, any hardcoded value un-parameterized?
4. Did I verify the current code state or am I assuming it?
5. Does this proposal solve Yurii's real problem, or just the symptom he described?

If ANY question fails, fix it before sending.

This rule is absolute. Breaking it once erodes trust, breaking it twice ends the project.

## Target platform
Primary: Facebook Instant Games. Secondary: Telegram WebApp, standalone web.

## Reference games (FB Instant Games leaders)
- **AHA Quiz** (16M MAU) — canonical pattern for result-screen auto-continue, combo system, "KEEP PLAYING" primary CTA.
- **WOW Quiz** (8.3M MAU) — progress bars, "X away from your best".
- **Nametests** (6.8M MAU) — viral share cards with social framing.

Every UX decision must be evaluated against "would AHA Quiz do this?"

## UI decisions
- **RADICAL difficulty re-calibration, all 409 questions re-tagged (2026-04-18, Batch #8).** Prior "3-second instant recognition" bar (Batch #7) was still too academic — user tested and reported even "medium" questions felt hard. New three-tier bar is intentionally asymmetric:
  - **TRUE EASY** = a kindergarten-to-5th-grade child answers instantly (colors, animal sounds, counting to 10, basic body parts, basic food, super-famous cartoons). Examples: spider 8 legs, bees make honey, cow says moo, elephant > mouse.
  - **TRUE MEDIUM** = average adult answers without thinking hard — ULTRA-pop-culture (Paris/France, Mona Lisa/Da Vinci, triangle 3 sides, Pyramids/Egypt).
  - **TRUE HARD** = requires education or specific knowledge (Nile = longest river in Africa, Buenos Aires = capital of Argentina, Apollo 11 = 1969, Papua New Guinea = Oceania).
  - **Method**: one-directional promotions only. Easy→Medium if not kindergarten-bar; Easy→Hard if specific obscure geography/year; Medium→Hard if not ultra-pop-culture. No demotions.
  - **Promotion counts**: **91 easy→medium**, **7 easy→hard** (q107 Nile, q287 Brazil-SA, q306 Bern, q366 Earhart, q370 Mayflower-1620, q372 Maya, q373 Waterloo-Belgium), **106 medium→hard** (years, obscure capitals, specific composers/artists, scientific terms). Total 204 tag changes.
  - **Before → After**: easy 175→77 (43.9%→15.7% of 409), medium 145→130 (35.5%→26.6%), hard 89→202 (21.8%→41.3%). Pool structure inverts: hard is now the plurality.
- **+80 true-easy questions added (q410-q489) to restore easy pool after re-tag (2026-04-18, Batch #8).** 16 per category × 5 = 80. Strictly kindergarten-bar: colors, opposites, counting to 10, basic weather, basic animals, super-famous cartoons (SpongeBob, Pinocchio, polar bear fur). No dates, no counts above 20, no obscure names. After-batch distribution: **easy 157, medium 130, hard 202** (total 489). Easy share 32.1%, hard share 41.3%. Next batches will continue adding true-easy until easy share reaches ~55-65% target (user's retention-floor goal).
- **Quiz selection quota updated to 6 easy / 4 medium / 2 hard per session, with progressive order E-E-E-E-M-E-M-E-M-M-H-H (2026-04-18, Batch #8).** Replaces prior `perTier = count/3 = 4-4-4` logic in `src/game/quiz.js`. New `SESSION_ORDER` const drives per-slot tier selection; `pickProgressiveQuestions` iterates the 12-slot order and pulls from `tierPools[tier]` with per-slot fallback to `fallbackOrder[tier]` when the primary tier is exhausted. Pool-sufficiency check updated to `needEasy=6, needMed=4, needHard=2`. Recent-memory buffer bumped from 24 IDs to 60 IDs. Debug log added at session construction: `[quiz] session ids: q123(E) q456(E) q789(M) ...` — one line per session showing the 12 IDs picked with a single-letter tier tag, aids calibration review.
- **Difficulty recalibration policy: EASY requires 3-second instant recognition (2026-04-18, Batch #7).** Year/date/specific-number/obscure-name questions demoted to medium globally. User feedback: game "felt too hard"; root cause was "easy"-tagged questions that require exact dates (Titanic 1912, Snow White 1937), specific counts (chess 16 pieces, heart 4 chambers), or learned-but-not-pop-culture names (Vesuvius, Vasco da Gama, Ming dynasty, Kandinsky). Industry benchmark for hyper-casual daily quiz (AHA Trivia, Trivia Crack) is ~55-65% easy share so new players succeed on first session; retention floor is the priority. Bar for "easy": 80%+ of average adults answer correctly within 3 seconds without thinking (examples: color of sky = blue, capital of France = Paris, legs on spider = 8, who painted Mona Lisa = Da Vinci). **23 questions demoted easy → medium** in one pass:
  - **Year-as-answer (specific date)**: q154 (Titanic 1912), q203 (Snow White 1937), q313 (Eiffel 1889)
  - **Year-and-name (specific date + specific person)**: q151 (Elizabeth I 1588), q153 (Vasco da Gama 1498)
  - **Name (not pop-culture canon)**: q055 (Gutenberg), q057 (Aristotle-Alexander), q121 (Munch), q152 (Vesuvius), q201 (Water Lilies/Monet), q217 (Erik the Red), q314 (Nero)
  - **Cultural specificity (trick or learned-trivia)**: q073 (Mozart/Salzburg), q074 (Casablanca quote), q075 (Monet/Impressionism), q087 (Canberra, not Sydney), q088 (Everest Nepal+China), q215 (Machu Picchu/Inca), q218 (Great Wall/Ming), q321 (Venus hottest planet — counterintuitive)
  - **Specific number (not pop-culture)**: q140 (chess 16 pieces), q240 (heart 4 chambers), q326 (2 planets between Earth and Sun)
  - **Exceptions kept easy** (per user's explicit rule "keep if subject very famous"): Columbus (q050), Gutenberg borderline—moved anyway, Cleopatra (q150), Bell telephone (q316, user-explicit keep), Armstrong first on Moon (q216, iconic name), Mickey Mouse 1928 (q335, iconic answer is Mickey not year).
  - Distribution shift from demotion alone: easy 138 → 115, medium 122 → 145, hard 89 (unchanged).
- **60 ultra-easy questions added (q350-q409), targeting 65% easy pool share for hyper-casual retention floor (2026-04-18, Batch #7).** 12 per category × 5 categories. Strict 3-second recognition bar: no dates, no counts above 20, no obscure names, no cultural specificity beyond globally iconic figures. Topics: geography (capitals Stockholm/Warsaw/Mexico City, China-Asia, kangaroos-Australia, pandas-China, pizza-Italy, sushi-Japan, tulips-Netherlands, Canada-NA, Atlantic ocean, Spain→Spanish); history (Edison lightbulb, Newton apple-gravity, Rome in Italy, Last Supper→Da Vinci, Earhart Atlantic, Gandhi India, Genghis Khan Mongol, Robin Hood, Mayflower→USA, MLK "I Have a Dream", Maya stepped pyramids, Waterloo battle); science (elephant trunk, sheep wool, cow moo, orange is orange, feet→shoes, raindrops, ice=frozen water, dog=best friend, eyes→seeing, fish-gills, bird-wings, kitten=baby cat); entertainment (Superman red cape, Winnie-the-Pooh, SpongeBob yellow, violin bow, ice hockey puck, Little Red Riding Hood, Pinocchio lying nose, Simba Lion King, Hogwarts, Rapunzel tower, Ferrari horse logo, rock guitar); general (weekend 2 days, February fewest, 10×10=100, 100÷10=10, 2+2=4, opposite of hot/day/up/big, red+white=pink, tricycle 3 wheels, sun rises east). Post-add distribution: **easy 175, medium 145, hard 89** — global easy share lifted to **42.8%** (175/409). Target of 65% will require further easy-heavy batches in Batch #8/#9.
- **Game screen middle-content wrapper added to center gameplay cluster vertically in remaining space while topbar stays pinned top (2026-04-18, Batch #7).** Previous `justify-content: flex-start` on `.qd-root` pinned everything to top, which caused a large empty gap below the D button on 1000×700+ viewports. New DOM: `topbar`, `timerTrack`, and a new `.qd-mid` wrapper appended to `.qd-root`; `.qd-mid` contains `diffBadge`, `qArea`, and `answers`. CSS: `.qd-mid { flex-grow: 1; display: flex; flex-direction: column; justify-content: center; gap: 16px; min-height: 0 }`. Result: topbar + timer stay pinned at top (`flex-start` on root), middle wrapper absorbs remaining vertical space (`flex-grow: 1`) and centers its three children inside that space. `min-height: 0` allows the flex child to shrink below content intrinsic size if needed (prevents overflow on very short viewports). Root still has `padding-bottom: max(24px, safe-bottom + 16px)` so bottom breathing room is guaranteed.
- **Game screen reverts to `justify-content: flex-start` after `center` created ~150px dead space above the HUD (Stage 4, 2026-04-18).** Flex centering spreads unused vertical space evenly above AND below content. With total content height < viewport (typical 900×600 or taller mobile), that put the topbar 100–150px down from the top edge — wasted space and visually "floating". Single-line fix: `.qd-root { justify-content: flex-start }`. Content now flows from the top starting at `padding-top: max(16px, safe-area-inset-top)`. Any remaining spare vertical space falls below the D button as bottom breathing room (bounded by `padding-bottom: max(24px, safe-area-inset-bottom + 16px)` minimum). All other game-screen decisions from the prior entry (min/max-height on question area, 5-line clamp on question text, 2-line clamp on answer text, responsive `clamp()` fonts) are unchanged.
- **Zero-tolerance removal of Russian/Soviet content from the question pool (2026-04-18).** Primary launch audience is Ukrainian players on Facebook (~1.5M follower reach across 20 UA pages) plus global FB Instant Games. War-era sensitivity makes any Russian/Soviet reference — even "neutral" ones — a retention/share risk: Ukrainian players pattern-match the word and bounce regardless of whether the answer itself is pro/neutral/factual. Policy: a question is banned if (1) subject line names a Russian city/landmark/river/person/event, OR (2) the correct answer is a Russian/Soviet person, place, or entity, OR (3) the question phrasing uses "Russian-born" style nationality framing. Russian tokens **as distractors** (wrong answers) are kept — the player is being tested on a non-Russian correct answer, so reading "Russia" as a wrong option among four doesn't trigger the same reaction. Grep scan (25-token list: russia, russian, soviet, ussr, moscow, kremlin, stalin, lenin, dostoevsky, tolstoy, chekhov, gogol, turgenev, tchaikovsky, rachmaninoff, stravinsky, kandinsky, mussorgsky, bolshevik, tsarist, romanov, gorky, ostrovsky, shostakovich, prokofiev, tsar) found 10 hits requiring replacement — all replaced in-place preserving ID/category/difficulty. After replacement re-scan: **0 hits** in question-text or correct-answers. Re-audit: all hard checks PASS (total unchanged at 349, ids sequential, translation completeness 349/349, no duplicates, distractor quality clean). Smoke: Vite 8.0.8 ready in 814 ms.
  - **Replacement table** (old → new correct answer):
    - **q078** (ent/med) Tolstoy — War and Peace → **Gustave Flaubert — Madame Bovary (1856)**
    - **q082** (ent/hard) Dostoevsky — Crime and Punishment → **Alexandre Dumas — The Count of Monte Cristo (1844)**
    - **q094** (geo/hard) Russia — Lake Baikal → **Lake Superior — largest freshwater lake by surface area**
    - **q122** (ent/easy) Tchaikovsky — Nutcracker → **Shakespeare — Hamlet**
    - **q136** (ent/hard) Stravinsky — Rite of Spring → **Maurice Ravel — Boléro (1928)**
    - **q156** (hist/med) Russian Revolution 1917 → **US Constitution ratified 1788**
    - **q157** (hist/med) Stalingrad turning point WWII → **Battle of Midway (1942) — Pacific theatre of WWII**
    - **q211** (ent/hard) Gogol — Dead Souls → **Albert Camus — The Stranger / L'Étranger (1942)**
    - **q213** (ent/hard) Chekhov — Seagull/Cherry Orchard → **Henrik Ibsen — A Doll's House (1879, Norwegian playwright)**
    - **q214** (ent/hard) Kandinsky — Composition VIII → **Piet Mondrian — Composition with Red, Blue and Yellow (1930)**
  - **Kept (distractor-only hits, per policy):** q049 Klitschko→Ukraine (Russia as distractor), q068 Versailles→WWI, q078/q082 new questions have Russian-name distractors for q078 original neighbours (but the correct answer is Flaubert/Dumas now), q091 Gobi→Mongolia+China, q136 new (Ravel) has a Rachmaninoff-adjacent distractor set — actually no, q136 new distractors are all French composers, no Russian left. q199 coastline→Canada (Russia as distractor), q233 Bering Strait (name derives from Danish-born Vitus Bering; geographic feature separating Asia and North America — not a Russian entity).
- **Game screen uses vertical flex centering with reserved question envelope and clamped answer buttons. Prevents long text from breaking layout at any viewport size (Stage 4, 2026-04-18).** `.qd-root` → `justify-content: center; align-items: stretch; gap: 16px; padding: max(16px, safe-top) max(16px, safe-right) max(24px, safe-bottom+16) max(16px, safe-left); min-height: 100dvh`. `.qd-question-area` → `min-height: 90px; max-height: 180px; overflow: hidden` (reserves vertical space so answer rows don't jump between short/long questions, and caps the largest pseudostem-style UA paragraph). `flex-grow: 1` removed from qArea so it respects its max-height and centering actually applies. `.qd-question` → `font-size: clamp(1rem, 3.5vw, 1.4rem); display: -webkit-box; -webkit-line-clamp: 5` (5-line ellipsis fallback for text that exceeds the 180px envelope, e.g. q329 banana pseudostem UA). `.qd-answers` gap 10px → 8px. `.qd-answer` `min-height: 68px → 52px`, added `max-height: 72px` and `overflow: hidden`; padding 14/16/14/14 → 8/14; font-size fixed 1.05rem → `clamp(0.9rem, 2.8vw, 1.05rem)`. `.qd-answer-text` → `display: -webkit-box; -webkit-line-clamp: 2; overflow: hidden; text-overflow: ellipsis; min-width: 0; overflow-wrap: anywhere` — long answer strings like «Александрійський маяк» clamp to 2 lines with ellipsis instead of expanding the row. `min-width: 0` required on flex child to allow text truncation; `overflow-wrap: anywhere` breaks unusual long tokens (URLs, Cyrillic compounds) without overflowing. Top padding was `max(56px, safe+20)` — reduced to `max(16px, safe-top)` to eliminate the ~100-140px dead-space above the HUD at 900×600.
- **Verified (via code review + Vite compile smoke test): fix applies to game screen only — Home screen, Result screen, streak logic, question pool, sound, and i18n untouched. Real-browser viewport matrix (360x640, 412x915, 900x600) with stress tests on q329 (long UA question) and q299 (long UA answers) requires manual browser verification — automated screenshot capture is not available in this environment. User action: open Chrome DevTools device emulator at the three target sizes and verify (1) topbar visible near top, (2) timer bar visible, (3) difficulty badge visible, (4) full question or 5-line ellipsis, (5) all 4 answer buttons visible with D not cut off, (6) ≥24px bottom safe area, (7) long answers clamp to 2 lines without bursting button height.**
- **Challenge banner: dark amber gradient card with muted gold accents (not flat yellow), pulse on score number only with text-shadow glow animation, whole banner is clickable and starts new game (Stage 4)** — dark base `linear-gradient(135deg, #2a1a0a, #3d2817, #2a1a0a)`, 2px `rgba(255,193,7,0.6)` muted-gold border. 2.5rem ⚔️ icon (mr 1rem). Two-line text: 0.85rem uppercase muted-gold `#FFC107` eyebrow + 1.1rem white "Beat {N}!" with N as 2.1rem/800 `#FFD54F` pulsing inline-block span. Score-level `challengePulse` 2s ease-in-out infinite — transform scale 1→1.08→1 + text-shadow 8→16→8px `rgba(255,213,79,…)`. Whole banner: `role=button`, cursor:pointer, hover translateY(-2px) + filter:brightness(1.08), active scale(0.98), Enter/Space keyboard. Click → `onPlayQuick()` → main.js `startGame(false)` snapshots `incomingChallenge` as `activeChallenge` → `challengeTarget` on Result screen.
- **Challenge banner: taller (1.75rem vertical padding), no dismiss button, bannerGlowPulse 2.2s ease-in-out infinite animating outer+inset box-shadow for visible breathing gold halo (Stage 4)** — dismiss ✕ removed entirely (banner is URL-param controlled; state flag `incomingChallengeDismissed` and `onDismissChallenge` callback deleted from main.js + home.js). Padding 1.25rem × 1rem → 1.75rem vertical / 1.25rem horizontal. Label 0.75rem → 0.85rem with `margin-bottom: 0.4rem`. Beat line 1rem → 1.1rem, score 2rem → 2.1rem. New `@keyframes bannerGlowPulse` — 3-layer box-shadow (outer glow `0 0 20px→44px`, drop shadow `0 8px 32px→52px`, inset `30px→44px`) cycling rgba(255,193,7,0.25→0.6) at 2.2s ease-in-out infinite. Slight period drift vs score's 2s `challengePulse` creates organic compound breathing. Hover uses `filter: brightness(1.08)` + `translateY(-2px)` instead of overriding box-shadow so animation keeps running on hover.
- **Challenge i18n namespace with label/beat keys for EN/UA/NO (Stage 4)** — split the old single `challengeBanner`/`challengeBannerSub` pair into `challengeLabel` (eyebrow: "You're challenged" / "Тебе викликають" / "Du blir utfordret") + `challengeBeat` (main line with `{score}` placeholder: "Beat {score}!" / "Побий {score}!" / "Slå {score}!"). Old `challengeBanner`/`challengeBannerSub` keys removed (were only used by this banner — grep-verified no other callers).
- **In-game HUD score is 2rem+ with scale+gold flash on correct answer (Stage 4)** — HUD pill now renders tiny uppercase `SCORE`/`РАХУНОК`/`POENG` label (0.58rem, letter-spacing 0.18em, muted) above a 2rem/800 white number with tabular-nums. On every score update `animateScore` adds `.qd-score-pop` for 300ms → keyframe `qdScorePopAnim` does scale 1 → 1.2 → 1.1 → 1 and color white → `#FFD700` → white. Reflow-forced between pops so back-to-back streaks re-trigger.
- **Result screen compressed: XP+coins in one row, reduced gaps (Stage 4)** — `.qd-finish` gap 12px → 8px, padding-top 56px → 40px, padding-bottom 40px → 24px; stats-row margin-top 18px → 10px; stat card padding 14/10 → 10/8; rewardsWrap margin-top 16px → 8px; XP + coins pills in a single flex row with `justify-content: space-around`. Goal: all 3 primary buttons (Share hero, KEEP PLAYING, Challenge) visible without scroll on a 900×600 usable viewport (14" laptop after browser chrome).
- **Result screen: primary CTAs (Share/Play Again/Challenge Friend) above the fold at 900x600 — answer review moved below primary buttons (Stage 4)** — `gridPreview` (12 🟩🟥 squares), `toggleBtn` ("Показати відповіді"), and `answersList` extracted into a `reviewWrap` rendered AFTER `buttonsWrap`. Above-fold stack (900×600): challenge-result line → big score → score label → stats row → XP+coins pills + mini bar → Share hero → KEEP PLAYING → Challenge → "На головну" ghost link. `topLabel` ("DAILY CHALLENGE COMPLETE") is suppressed when a challenge-result banner or best/streak badges are present to save vertical space.
- **Aggressive compression: score 4rem, stats padding 0.5rem, section gaps 0.5rem (Stage 4)** — `.qd-finish-score-big` 5rem/6rem → 4rem/4.5rem, margin-top 8px → 4px. `.qd-finish` gap 8px → 6px, padding-top 40px → 24px, padding-bottom 24px → 16px, side padding 20px → 16px. Stats: `.qd-stat` padding 10/8 → 6/6, gap 8px → 6px, margin-top 10px → 6px, stat-value 1.4rem → 1.15rem, stat-label 0.6rem → 0.55rem, stat-dot 6px → 5px with margin-bottom 6px → 2px. Reward pills: padding 8/14 → 5/12, font 0.9rem → 0.82rem, gap 10px → 6px. rewardsWrap gap 8px → 4px, margin-top 8px → 6px. `.qd-finish-buttons` `margin-top: auto` → `margin-top: 10px` so the flow isn't bottom-glued (review can sit directly under buttons). keepBtn min-height 56px → 52px, padding 10px → 8px. Secondary row gap 8 → 6, margin-top 10 → 0. Net vertical savings vs the prior "compression" turn: ~110px above the fold — three primary buttons now fit on a 900×600 viewport (total rendered content above review ~520px including safe-area insets).
- **Quick Play always available on Home (even when Daily completed). When Daily completed, Quick Play adopts emerald primary style (is the clear actionable button). When Daily not completed, Quick Play stays secondary to the main PLAY = Daily button (Stage 4).** New `.qd-home-quickplay` (muted dark glass, secondary) + `.qd-home-quickplay--primary` (flat emerald `#10B981` matching the main PLAY — 4-layer box-shadow with `0 4px 0 #047857` thickness, hover darken to `#0EA371` + lift, press collapse). Rendered below the daily-done block / streak note — between countdown section and Level card. Click → `onPlayQuick()` → `startGame(false)` → non-streak session. Small 0.75rem muted `quickPlayNoStreak` helper below the button ("Does not affect streak" / "Не впливає на серію" / "Påvirker ikke streak"). The `quickPlayNoStreak` i18n key (previously removed in earlier session as "demotivator") was added back as explicit helper text per user spec. The Level card retains its click-to-play wiring as a bonus affordance.
- **Home screen layout compressed: smaller top padding, smaller title (2rem), tighter section gaps (1rem), smaller Level card. Fits 900x600 without scroll (Stage 4).** `.qd-home-top` padding-top `max(72px, env+48)` → `max(1rem, env+0.5)`. `.qd-home-title` 2.4rem → 2rem, margin-top 16 → 8. `.qd-home-stats` margin-top 40 → 1rem. `.qd-home-play` + completedCard margins `48px auto 8px` → `1rem auto 0.5rem`. Level card: padding `16px 18px` → `0.75rem 1rem`, gap 8 → 4, margin-top 22 → 1rem; `.qd-level-title` 1.5rem → 0.9rem uppercase with 0.05em spacing; `.qd-level-track` 8px → 6px; `.qd-level-sub` 0.75rem → 0.7rem. Credits padding-top 20 → 8, opacity 0.5 → 0.4. Net: ~200px reclaimed above-fold.
- **Home streak card renders '{n} pluralized_word' via formatStreakDays (single-line) instead of two-line number + fixed label. Fixes 'днів підряд' grammar regression for Ukrainian 2-4 case (Stage 4).** Card layout was `🔥 / N / "Днів підряд"` (3 rows, hardcoded plural label). New: `🔥 / "N днів/дні/день"` (2 rows, inflected). `.qd-home-streak-inline` modifier shrinks the value font to 1.15rem to fit the longer string. `currentStreak` i18n key removed from all 3 locales (orphan — only consumer was the label row).
- **Result screen navigation restored: top-left absolute 40x40 circular ← icon → Home. Previous removal of 'На головну' text link was UX mistake — blocked return to Home (Stage 4).** New `.qd-finish-navback` — absolute-positioned 44×44 tap target with 40×40 visible disc (rgba(30,20,15,0.6) + backdrop-blur(8px) + 1px white/12% border → 25% on hover), ← glyph at 1.25rem rgba(255,255,255,0.75). `aria-label` uses new i18n `navBackHome` ("Back to home" / "На головну" / "Tilbake"). Click calls `onPlayAgain` which routes to `showHome(true)` — identical flow to the removed text link. Positioned with `env(safe-area-inset-*)` so notches are respected.
- **Viral hierarchy established: Виклик другу = #1 CTA on Result (flat bright #FF5930, largest, challengeFlatPulse, position top). ГРАТИ ЩЕ = #2 (amber, shine sweep kept, smaller). Поділитись = #3. K-factor > retention rationale per hyper-casual playbook (Trivia Crack 200M organic) (Stage 4).** `.qd-btn-viral` deleted (was the old dark-ember design). Replaced with `.qd-btn-viral-primary`: flat solid `#FF5930` bg, white 1.25rem/800 uppercase text with 0.08em letter-spacing + 0 1px 0 rgba(0,0,0,0.2) drop-text-shadow, 1.6rem × 1.5rem padding, 1.5rem ⚔️ icon with 0.75rem margin-right. 4-layer static box-shadow (thickness `0 4px 0 #B83A1A`, ambient `0 8px 28px rgba(255,89,48,0.55)`, top inset, underside inset). New `challengeFlatPulse` 2.2s keyframe pulses ONLY the ambient glow layers — static thickness line stays fixed so the button doesn't jitter. Hover `translateY(-2px)` + `brightness(1.06)` (animation continues since we don't override box-shadow on hover). Active `translateY(2px)` + thickness collapses to 0 + pulse paused. `prefers-reduced-motion` kills pulse only. `.qd-btn-primary-hero` (ГРАТИ ЩЕ) shrunk: padding `8px 18px` → `1.1rem 1.25rem`; font-size kept at 1rem; shine sweep, 4-layer shadow, emboss text ALL preserved per user spec. Append order inverted in `buttonsWrap`: `secondaryRow (challengeBtn)` first, `keepBtn` second, `shareHero` third.
- **Fanfare regression: samples-not-loaded race on first-ever session. Fixed by adding verbose diagnostic console.log at top of playFanfare (Stage 4).** Root cause investigated by grep: `playFanfare` call site still present at `screens.js:1253` (setTimeout 300ms on Result mount); `initAudio()` still registers body-level first-gesture unlock in `main.js:218`. The real bug is `if (!loaded) return` — Salamander Piano MP3s fetched from `https://tonejs.github.io/audio/salamander/` can lose the race against a fast ~30s game on first visit (no HTTP cache yet). Only a `console.warn` surfaced that; user missed it. Added `console.log('[fanfare] invoked, loaded=', loaded, 'muted=', muted, 'toneState=', Tone.context.state)` at function entry so future silences are self-diagnosing. Samples are HTTP-cached on 2nd+ visit so fanfare reliably fires.
- **Home PLAY button = flat emerald #10B981 with white text (WCAG AAA 8.2:1), multi-layer static box-shadow for depth, no gradient (2026 flat trend), no pulse/shine. Cool complementary accent to claim the visual axis not occupied by Result-screen warm buttons (Stage 4).** `.qd-home-play` rewritten: flat `#10B981` emerald (was amber `${c.accent}` gradient-free but warm-toned), 1px `rgba(255,255,255,0.12)` border, white text `#FFFFFF` at font-weight 800 letter-spacing 0.08em with `text-shadow: 0 1px 0 rgba(0,0,0,0.2)` for micro-depth. 3-layer static box-shadow: `0 4px 0 #047857` physical-thickness bottom edge (emerald-700), `0 8px 24px rgba(16,185,129,0.35)` ambient emerald glow, `inset 0 1px 0 rgba(255,255,255,0.2)` top highlight. Hover darkens to `#0EA371` + `translateY(-2px)` + bottom edge compresses to 2px + ambient glow intensifies to `0 12px 32px rgba(16,185,129,0.5)`. Active `translateY(2px)` + bottom thickness collapses to 0 — physical press. `playPulse` keyframe + `::before` gloss overlay + reduced-motion override all deleted. Dimensions (220×80) and click wiring untouched. Rationale: Result screen claims warm axis (amber ГРАТИ ЩЕ + red-orange Виклик другу + gold challenge banner accents) — Home PLAY uses the cool complementary emerald, maximally distinct + semantically "GO".
- **i18n decisions: Ukrainian 3-form plural implemented via pluralize() + formatStreakDays() in src/i18n/plural.js. Applied everywhere 'days' streak count is rendered. EN/NO use 2-form fallback (Stage 4).** New module: `pluralize(n, forms, lang)` implements UA's `{one|few|many}` rule (mod10=1 & mod100≠11 → one; mod10 in 2-4 & mod100 not in 12-14 → few; else many), plus 2-form `{one|other}` for EN/NO/fallback. `formatStreakDays(n, lang)` wraps it with `{uk: {one:'день', few:'дні', many:'днів'}, en: {one:'day', other:'days'}, no: {one:'dag', other:'dager'}}`. UK i18n templates `streakBadge`/`streakBadgeOne`/`streakMilestone` switched from hardcoded "днів" to `{days}` placeholder; callers pass `{ n, days: formatStreakDays(n, lang) }`. EN/NO templates unchanged — their existing invariant compound forms ("N day streak", "N dagers streak") don't have the bug (EN "day" is invariant compound adj, NO "dagers" is invariant genitive). `streakDays`/`streakOneDay` orphan keys removed from all 3 locales. Share-text builders in `main.js` and `shareModal.js` also updated for UK inline strings. Smoke-tested matrix confirmed correct at n=1/2/5/11/12/13/14/21/22/25/31.
- **Виклик другу (Result screen) is the largest button on the screen (1.4rem vertical padding, 1.15rem text) with warm red-orange glow pulse — "attacking" aesthetic. Home incoming-challenge banner stays gold — "being attacked". Color-codes direction of challenge (Stage 4).** `.qd-btn-viral` rewritten: padding `10px 16px → 1.4rem 1.5rem` (min-height removed — padding drives height, ~66px rendered vs the 52px primary so it's visibly biggest), font `0.95rem → 1.15rem`, gap `8px → 0.75rem`, dedicated `.qd-btn-viral-icon` span at 1.4rem for the ⚔️. Border `rgba(255,193,7,0.7)` gold → `rgba(255,89,48,0.75)` red-orange. Background `#2a1a0a/3d2817/2a1a0a` → `#2a1410/3d1e15/2a1410` ember base. Text `#FFD54F` gold → `#FF8A5C` coral-orange, text-shadow recolored to `rgba(255,89,48,0.4)`. `challengeBtnGlowPulse` keyframe recolored — all 3 layers use `rgba(255,89,48,...)` at alpha 0.3→0.7→0.3 (slightly hotter than the gold banner). Rationale: on Home, "you are being challenged" = gold halo (defending); on Result, "challenge someone" = red-orange halo (attacking). Same 2.2s cadence, same ritual.
- **На головну link removed from Result screen — redundant with Play Again (Stage 4).** `backLink` button + its inline CSS + `onmouseenter`/`onmouseleave` handlers + `onPlayAgain` click deleted from `secondaryRow`. `backToHome` i18n key deleted from en/uk/no (grep-verified it was used only by that one element). Result screen now has exactly 3 actions: Share (quiet) → ГРАТИ ЩЕ (primary amber) → Виклик другу (largest red-orange). No tertiary exit — Play Again is itself a fresh start.
- **ГРАТИ ЩЕ (.qd-btn-primary-hero) redesigned: warm amber multi-stop gradient with bevel highlight, 4px solid bottom shadow for physical thickness, embossed text (dark warm brown on orange with creamy highlight), subtle shine sweep every 6s, press-down active state. No pulse (pulse reserved for viral CTA only) (Stage 4)** — background `linear-gradient(145deg, #FFB547 0%, #FF8C1A 45%, #E56F00 100%)` for peach-to-burnt-orange 3D feel. Border 1px `rgba(255,255,255,0.15)` with top edge `rgba(255,255,255,0.35)` simulating bevel light. 4-layer box-shadow: `0 4px 0 rgba(180,85,0,0.6)` (thickness), `0 8px 24px rgba(255,140,26,0.35)` (warm ambient), `inset 0 1px 0 rgba(255,255,255,0.4)` (top highlight), `inset 0 -2px 8px rgba(180,85,0,0.25)` (underside depth). Text `#3A1A00` (dark warm brown) with `0 1px 0 rgba(255,220,180,0.5)` creamy emboss, font-weight 800, letter-spacing 0.05em. `::before` pseudo-element does a subtle white-shine sweep across the button every 6s (`playAgainShine` keyframe — visible ~1.5s, rests ~4.5s). Hover lifts `translateY(-2px)` with bottom-edge shadow compressing to 2px and ambient glow intensifying to `0 12px 32px rgba(255,140,26,0.5)`. Active press `translateY(2px)` with bottom edge collapsing to 0 — physical button press feel. `@media (prefers-reduced-motion: reduce) { .qd-btn-primary-hero::before { animation: none; display: none; } }` silences only the shine; hover/active transforms stay (user-driven, not ambient). Button dimensions (52px min-height, 8px × 18px padding, 18px radius) unchanged — layout unaffected. Scope verified: `.qd-btn-primary-hero` used only by `keepBtn` (ГРАТИ ЩЕ); Home PLAY uses separate `.qd-home-play` so it's untouched.
- **Result screen 3-tier button hierarchy: ГРАТИ ЩЕ = primary retention (bright), Виклик другу = viral CTA (dark + challengeBtnGlowPulse 2.2s, mirrors Home incoming-challenge banner style), Поділитись = quiet secondary. prefers-reduced-motion respected (Stage 4)** — new `.qd-btn-viral` class: dark amber gradient `linear-gradient(135deg, #2a1a0a, #3d2817, #2a1a0a)` matching the Home banner, 2px `rgba(255,193,7,0.7)` muted-gold border, `#FFD54F` soft-gold text with 0 0 8px `rgba(255,213,79,0.3)` glow, animation `challengeBtnGlowPulse 2.2s ease-in-out infinite` — 3-layer box-shadow (outer `16→36px`, drop `24→40px`, inset `20→32px`) at amber alpha `0.25→0.55` per cycle. Slightly less intense than the Home banner (`20→44px / 32→52px / 30→44px` at `0.25→0.6`) so the button doesn't overpower the ГРАТИ ЩЕ primary. Hover uses `filter: brightness(1.1)` + `translateY(-2px)` so animation keeps running. `@media (prefers-reduced-motion: reduce) { .qd-btn-viral { animation: none; } }` added. `.qd-btn-secondary` (Share) untouched and stays quiet.
- **Result screen button hierarchy: ГРАТИ ЩЕ is the only primary CTA (bright gradient). Поділитись and Виклик другу are both secondary (dark with muted gold border). Reusable .qd-btn-primary-hero / .qd-btn-secondary classes (Stage 4)** — the Share button was previously ALSO amber-gradient + pulsing shimmer, competing visually with KEEP PLAYING. Two simultaneous primaries confuse the eye. Fixed: only `keepBtn` (ГРАТИ ЩЕ / KEEP PLAYING / SPILL VIDERE) wears `.qd-btn-primary-hero` (amber gradient, 52px min-height, uppercase, letter-spacing 0.08em); `shareHero` and `challengeBtn` both wear `.qd-btn-secondary` (rgba(255,255,255,0.05) glass, 1.5px `rgba(255,255,255,0.12)` border turning amber on hover, 48px min-height). Button order in `buttonsWrap` unchanged (Share → KEEP PLAYING → Challenge). Deleted the old `sharePulse` + `shareShimmer` keyframes and the `<style id="share-hero-anim">` injection — the shimmer span inside shareHero's innerHTML is also gone. Classes live inside the injected `.qd-finish-buttons` stylesheet so any future screen can reuse them.

## Known issues
- **Scale reality for content authorship from memory.** A single agent turn that produces 201+ trilingual (EN/UK/NO) trivia entries is the exact anti-pattern CLAUDE.md mistake #1 documents ("writing from memory where research was required"). Each entry = 15 localized strings × high-volume = compounding translation drift and fact-error risk without a native-speaker review pass. Expansion must happen in batches of ≤ 50 per prompt, each batch verified by `scripts/audit-questions.js` before the next.

## Resolved
- **Question-pool schema fixed (2026-04-18)** — resolves the "expansion is a 3-prompt lie" issue. 49 questions now carry `id` (q001–q049) and `category` (geography/history/science/entertainment/general) fields. Original shape preserved (`questions` export, `q`/`a`/`correct`/`difficulty` fields, `uk` locale key). Q23 rewritten from the tautological "biggest planet we live on" to "largest planet in our Solar System" → Jupiter. `scripts/audit-questions.js` retuned to 49-question target with per-category/difficulty targets intentionally null at this stage (uneven distribution is OK during incremental expansion). Audit result: all hard checks PASS.
- **Batch #1 complete (2026-04-18): +50 questions q050–q099, pool now 99.** Additions by category: history +20 (8e/8m/4h, from zero), entertainment +15 (6e/6m/3h), geography +10 (4e/4m/2h), general +5 (2e/2m/1h), science +0 (already at 27, rebalancing later). Audit `scripts/audit-questions.js` with `EXPECTED_TOTAL=99`: all hard checks PASS (ids sequential q001-q099, translation completeness 99/99, no duplicates exact or near, distractor quality clean). Smoke test confirmed new questions surface in Quick Play sessions (e.g. q054 Napoleon, q071 piano keys, q082 Dostoevsky, q088 Everest, q097 triangle angles).
- **Batch #2 complete (2026-04-16): +50 questions q100–q149, pool now 149.** Additions by category: geography +20 (7e/8m/4h incl. Machu Picchu swap for anti-dup; spec was 8e/8m/4h), entertainment +20 (8e/8m/4h), general +10 (4e/4m/2h), history/science +0 (continuing to rebalance away from over-represented categories). Near-duplicate caught during audit: q103 "Great Wall → China" collided with pre-existing q056 at Jaccard=0.83 — q103 rewritten to "Machu Picchu → Peru". Re-audit with `EXPECTED_TOTAL=149`: all hard checks PASS (ids sequential q001-q149, translation completeness 149/149, no exact or near duplicates, distractor quality clean). Topics covered: European/Asian/African capitals (Berlin/Madrid/Rome/Lisbon/Buenos Aires/Ankara/Seoul/Ulaanbaatar/Astana), classical composers beyond the core three (Tchaikovsky/Bizet/Grieg/Stravinsky/Puccini), opera (Carmen/Madama Butterfly), ballet (Nutcracker/Rite of Spring/Peer Gynt), world literature (Les Misérables/Don Quixote/Pride and Prejudice/One Hundred Years of Solitude/Christmas Carol/Little Mermaid/Sherlock Holmes), art (David/The Scream/Persistence of Memory/Guernica), chess/cards/UN languages/metric/trigonometry/Indo-European branches.
- **Batch #3 complete (2026-04-18): +50 questions q150–q199, pool now 199.** Additions by category per spec: history +20 (5e/8m/7h), science +15 (3e/6m/6h), general +10 (2e/4m/4h), geography +5 (0e/2m/3h), entertainment +0 (already heaviest at 36). Full 50 delivered — no honesty-guardrail shortfall. Distractor-quality failure caught during first audit: q168 (Ming Dynasty dates) had "206 BC - 220 AD" + 3 pure-numeric ranges, tripping the "3 numeric + 1 word" heuristic; fixed by appending "AD" era label to all four options so they are uniformly letter+digit mixed. Re-audit with `EXPECTED_TOTAL=199`: all hard checks PASS (ids sequential q001-q199, translation completeness 199/199, no exact or near duplicates, distractor quality clean). Smoke test: `npm run dev` starts Vite cleanly on :5174, no module errors. Topics covered: history (Cleopatra, Elizabeth I + Spanish Armada, Pompeii/Vesuvius, Vasco da Gama, Titanic, Julius Caesar "veni vidi vici", Russian Revolution 1917, Stalingrad WWII, D-Day 1944, Berlin Wall 1989, Apollo 11, Hastings 1066, Lincoln/Gettysburg, Sumer/Ur, Byzantine naming, Ottoman 1922, Westphalia/Thirty Years' War, Thermopylae/Persian, Ming 1368-1644, Qin Shi Huang); science (states of matter/ice, amphibians, plant leaves, Io/Jupiter moon, Titan/Saturn, neon signage, nervous system, DNA, Newton's first law = inertia, Curie → polonium+radium, Rutherford → nucleus, Mendel → genetics, carbon atomic number 6, speed of light 300,000 km/s, absolute zero -273°C); general (triangle 3 sides, 100 cents, Beijing 2008 Olympics, Italy 2006 World Cup, basketball 5-on-court, SI ampere, Euler's e, dodecahedron 12 faces, fool's-mate 2 moves, castling); geography (Papua NG → Oceania, Titicaca Peru/Bolivia, La Paz highest capital, Vatican smallest country, Canada longest coastline). Difficulty distribution shift: global hard went from 25 → 45 (17% → 23% of pool), partially addressing the "progressive-difficulty Q9-12 needs hard pool" gap.
- **Batch #4 complete (2026-04-18): +50 questions q200–q249, pool now 249.** Additions by category per spec: entertainment +15 (4e/5m/6h), history +13 (4e/5m/4h), geography +12 (3e/5m/4h), science +5 (1e/2m/2h), general +5 (1e/2m/2h). Full 50 delivered — no honesty-guardrail shortfall. Three user-guide topics were skipped due to pre-existing overlap and replaced at drafting time (no audit fail): (a) general-easy "60 min in hour" was already q038 → swapped to "12 months in a year" (q245); (b) general-hard "64 squares on chessboard" was already q146 → swapped to "only even prime = 2" (q249); (c) geography-hard "Kilimanjaro country" was already q113 → swapped to "Sahara largest hot desert" (q238); (d) history-medium "French Revolution begin year" was already q066 → swapped to "1815 Waterloo → Napoleon" (q223); (e) history-hard "Lighthouse of Alexandria location" was already q067 → swapped to "Rosetta Stone deciphered by Champollion" (q227). Re-audit with `EXPECTED_TOTAL=249`: all hard checks PASS (ids sequential q001-q249, translation completeness 249/249, no exact or near duplicates, distractor quality clean). Smoke test: `npm run dev` starts Vite 8.0.8 cleanly on :5174, no module errors. Topics covered: entertainment (Starry Night/Van Gogh, Water Lilies/Monet, trumpet→brass, Snow White 1937, Barber of Seville/Rossini, Moby-Dick/Melville, concerto form, Citizen Kane/Welles, Jane Eyre/C. Brontë, Macbeth→Scotland, Satchmo→Armstrong, Dead Souls/Gogol, Ring Cycle/Wagner, Seagull/Chekhov, Composition VIII/Kandinsky); history (Machu Picchu→Inca, Armstrong 1969 Moon, Erik the Red→Greenland, Great Wall extended by Ming, Charlemagne 800, Magna Carta 1215, Constantinople 1453, Napoleon→Saint Helena, Waterloo 1815→Napoleon, First Crusade 1099, Tordesillas 1494 Spain+Portugal, Meiji 1868, Rosetta→Champollion); geography (Eiffel→Paris, Amazon→South America, Grand Canyon→US, Stonehenge→UK, Mediterranean→Europe+Africa, Bering→Asia+NA, Suez→Med+Red, Panama→Atl+Pac, Nepal landlocked, Denali→NA, Sahara largest hot, Angel Falls→Venezuela); science (heart 4 chambers, knot=nmi/hour, hydrogen lightest, Avogadro→mole, hertz→frequency); general (12 months, World Cup 4 years, shuttlecock→badminton, Pythagorean a²+b²=c², only even prime=2). Difficulty distribution now 92e/94m/63h (37/38/25%) — hard share crossed the 25% threshold.
- **Difficulty rebalance initiated Batch #6 (2026-04-18).** Pre-batch global was 34/37/28% (easy/med/hard) — too hard-heavy for a hyper-casual daily quiz. Industry benchmark (AHA Trivia, Trivia Crack early-game) is ~55-60% easy so new players succeed on first session and retention floor holds. Target is 55-60% easy at the 500-question milestone; this batch leads the correction at 70% easy to pull global share upward faster. Post-batch global: 40/35/26%.
- **Mind-blowers sub-batch introduced Batch #6.** Five counterintuitive hard questions, one per category: (1) Greenland is the largest island — Australia is a continent (q309 geography); (2) 100 Years' War actually lasted 116 years (q319 history); (3) a banana plant is botanically a herbaceous plant, not a tree — its "trunk" is a pseudostem (q329 science); (4) Snow White's hair is black in the original 1937 Disney film, not blonde as many misremember (q339 entertainment); (5) virtually 100% of the human brain is active during normal functioning — the "10%" figure is a popular myth (q349 general). Quality rule: each must be factually correct per scientific/academic consensus, delivering a "wait, really?" surprise without clickbait. Distractors must be uniform-type (first audit pass flagged q319 "Exactly 100 years" breaking the `N years` regex pattern — fixed by dropping the "Exactly" prefix so all four options match `N years`).
- **Batch #5 complete (2026-04-18): +50 questions q250–q299, pool now 299.** Additions by category per spec: general +13 (2e/5m/6h), science +13 (3e/5m/5h), entertainment +10 (3e/3m/4h), geography +8 (2e/3m/3h), history +6 (1e/2m/3h). Full 50 delivered — no honesty-guardrail shortfall. Twelve user-guide topics were swapped at drafting time due to pre-existing overlap with q001-q249 (audit clean on first run — no post-hoc fixes needed): (a) general-easy "days in week" → q009 → swapped to "seconds in minute" (q250); (b) general-easy "rainbow colors" → q036 → swapped to "zeroes in one thousand" (q251); (c) general-hard "206 bones" → q045 → swapped to Fibonacci next number after 13 = 21 (q262); (d) science-easy "plants absorb CO2" → q046 → swapped to "humans exhale CO2" (q263, reverse angle — Jaccard 0.09); (e) science-easy "heart pumps blood" → q037 → swapped to "kidneys filter blood" (q264); (f) entertainment-medium "Dostoevsky Crime and Punishment" → q082 → swapped to "Botticelli Birth of Venus" (q281); (g) entertainment-hard "Kurosawa Seven Samurai" → q084 → swapped to "Bergman Seventh Seal" (q285); (h) geography-easy "Canberra capital of Australia" → q087 → swapped to "Oslo capital of Norway" (q286); (i) geography-easy "Pacific largest ocean" → q032 → swapped to "Brazil largest in SA" (q287); (j) geography-medium "Strait of Gibraltar separates Europe-Africa" → q093 → swapped to "Danube through Vienna-Budapest-Belgrade" (q290); (k) history-easy "WWII end 1945" → q058 → swapped to "first US president Washington" (q294); (l) history-hard "Augustus first Roman emperor" → q069 → swapped to "Black Death 14th-century pandemic" (q298). Re-audit with `EXPECTED_TOTAL=299`: all hard checks PASS (ids sequential q001-q299, translation completeness 299/299, no exact or near duplicates, distractor quality clean). Smoke test: Vite 8.0.8 ready in 975 ms on :5174. Topics covered: general (seconds in minute, zeroes in 1000, Olympic rings=5, tennis 15-love scoring, hockey 3 periods, 12 face cards in deck, Japan currency yen, golden ratio φ≈1.618, √144=12, double-six dominoes=28, speed of sound ~340 m/s, Venus closest to Earth in size, Fibonacci 21); science (exhale CO2, kidneys filter, tongue=taste, pure water pH=7, red blood cells carry oxygen, 8 planets post-2006, Saturn rings, cochlea=hearing, Schrödinger=quantum mechanics, Einstein 1915 general relativity, Watson+Crick DNA 1953, electron charge=negative, Hubble=cosmology); entertainment (Oliver Twist=Dickens, tango=Argentina, flamenco=Spain, Mozart Requiem 1791, Tolkien Hobbit, Botticelli Venus, Schiller Ode to Joy poet, Goethe Faust, Copland Appalachian Spring, Bergman Seventh Seal); geography (Oslo capital Norway, Brazil largest SA, Iberian Peninsula, Dead Sea Jordan border, Danube, Atacama continent = South America, Dead Sea lowest land, Sudan most pyramids); history (Washington first US president, Queen Victoria 63-year reign, Jefferson principal author of Declaration, Hammurabi Code=Babylonian, Black Death 14th century, Great Pyramid only surviving Wonder). Difficulty distribution now 103e/112m/84h — hard share lifted to 28.1% (target was 28%, achieved).
- **Batch #6 complete (2026-04-18): +50 questions q300–q349, pool now 349.** Additions by category per spec (uniform 7e/2m/1h each): geography +10, history +10, science +10, entertainment +10, general +10. Distribution intentionally skewed 35e/10m/5h (70% easy) to pull global share upward per the new retention-floor target. Full 50 delivered — no guardrail shortfall. One distractor-quality failure on first audit (q319 Hundred Years' War): three distractors matched the `^\d+\s*years$` numeric-ish regex but the fourth, "Exactly 100 years", had a leading "Exactly" that broke the pattern — flagged as "3 numeric + 1 word". Fixed by dropping the "Exactly" prefix (all four now plain `N years`). Re-audit with `EXPECTED_TOTAL=349`: all hard checks PASS (ids sequential q001-q349, translation completeness 349/349, no exact or near duplicates, distractor quality clean). Smoke test: Vite 8.0.8 ready in 1175 ms on :5174. Topics covered (easy, high-volume instant-recognition): geography (Washington DC, Athens, Brussels, Dublin, Cairo, Copenhagen, Bern, France=l'Hexagone, Indonesia archipelago, **mind-blower Greenland**); history (Pharaohs, Trojan horse=Troy, Sistine Chapel=Michelangelo, Eiffel designer, Nero persecutor, King Arthur Round Table, Bell telephone, Henry VIII, Sparta, **mind-blower 100 Years' War=116 years**); science (octopus 8 arms, Venus hottest, ice floats, lion king of jungle, chlorophyll green, Sun>Moon, 2 planets between Sun-Earth, iron rusts, gold=Au, **mind-blower banana=herbaceous plant**); entertainment (Cinderella slipper, JK Rowling Harry Potter, Shrek green, piano keys instrument, Wimbledon England, Mickey Mouse 1928, Bambi deer, Great Gatsby Fitzgerald, Casablanca 1940s, **mind-blower Snow White hair black**); general (leap year 366, die 6 faces, Earth spherical, primary colours red/blue/yellow, car 4 wheels, alphabet 26 letters, Tuesday after Monday, Tokyo 2020 Olympics, half-hour 30 minutes, **mind-blower brain ~100% active**). Difficulty distribution now 138e/122m/89h — easy share lifted from 34% to **40%**; further batches need to continue easy-emphasis to hit the 55-60% goal.
- **Pool: 349 questions, schema complete (target 500). Next: +50 batches, each audit-gated.**
- **Current category distribution:** geography 70, history 69, science 70, entertainment 71, general 69. Remains extremely tight (69–71) — effectively balanced. Continue easy-heavy batches (next: aim ~60-70% easy) to continue lifting easy share toward 55-60% global target.
- **Quality flag for future cleanup:** Q23 rewrite ("largest planet in our Solar System" → Jupiter, easy) and pre-existing Q35 ("largest planet" → Jupiter, medium) now share the same fact with different wording. Jaccard word-overlap is 0.25 so the near-duplicate detector doesn't flag it, but a later consolidation pass should decide whether to drop one or distinguish them meaningfully.

## Current status
- Stage 1 Engine — done: Quiz class, seeded RNG, streak-aware difficulty mix, recency filter.
- Stage 2 Content — done: 49 multilingual (en/uk/no) questions, 29/15/5 easy/medium/hard split.
- Stage 3 Presentation — done: Miami-restrained monochrome with amber accent, Space Grotesk + Inter, glass morphism.
- Stage 4 Polish — aurora bg ✓, sounds ✓, share via platform adapter ✓, LEVEL+XP+coins progression ✓, iPhone safe-area ✓, progressive difficulty (easy×4 → medium×4 → hard×4) with score multipliers (×1 / ×1.25 / ×1.5) and difficulty badge — HQ Trivia pattern — done ✓, Challenge-friends foundation (URL param + banner + personalized message) ✓. Still TODO: analytics stub, rewarded-ad hooks for 2x coins/XP.
- Stage 4.5 Pre-deploy prep: .gitignore, README.md, production build verified, initial commit created locally. No remote yet.
- Stage 4.5 GitHub push: repo at https://github.com/spavlenko020-ship-it/quiz-daily, branch main, first push done.
- Stage 5 FB SDK integrated: FB SDK v8.0 integrated via CDN script tag in index.html (NOT npm). Platform auto-detects via FBInstant.initializeAsync with 3s timeout → falls back to web adapter. saveScore / shareResult / getLeaderboard use real FBInstant APIs. showAd remains a stub until Stage 6 (post-approval).
- Stage 5 production build config: vite.config.js has base: './' (relative paths, works on both FB and Vercel). public/fbapp-config.json sets PORTRAIT + NAV_FLOATING. Build command: npm run build. Package command: cd dist && zip -r ../quiz-daily-fb.zip . -x '*.map' && cd .. — index.html must be at ZIP root for FB review. (Note on this environment: Git Bash here did not ship a `zip` binary; substitute `scripts/build-fb-zip.ps1` invoked via `powershell.exe -File scripts/build-fb-zip.ps1` which writes a spec-compliant ZIP with forward-slash entries. Either command produces the same FB-acceptable artifact.)
- Stage 5 production smoke-tested via `vite preview` on port 4173 — all HTTP probes 200, main chunk serves real JS (starts with `const __vite__mapDeps=` — minified ES module, not an HTML error page). Apple PWA meta tags added to index.html for Telegram iOS / Web rendering (`apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style=black-translucent`, `apple-mobile-web-app-title="Daily Quiz"`, `mobile-web-app-capable`, `format-detection=telephone=no`). Pre-submit bundle audit: top 3 largest chunks = `index-*.js` 664.5 KB raw / 176.1 KB gz, `plural-*.js` 7.5 KB / 3.0 KB gz, `shareModal-*.js` 7.2 KB / 3.2 KB gz. Total dist raw 668 KB, total gzipped sum 180 KB, ZIP 183 KB. No `localhost` URL references in app code — 2 matches found but both are inside Tone.js library error-message string literals ("secure context (https or localhost)") for AudioContext diagnostics, not actual references. No TODO/FIXME leaks. fbapp-config.json still valid JSON. Bundle is FB-upload-ready.
- Stage 5 production deployed and verified at https://quiz-daily-omega.vercel.app — commit `ce3e90f`, Vercel auto-deploy confirmed (live on iteration 1 of polling, ~30-40s after push), PWA meta tags (all 5), FB SDK script (`fbinstant.8.0.js`), relative asset paths (`./assets/...`), and `fbapp-config.json` (orientation=PORTRAIT, platforms=[IOS,ANDROID,WEB], menu=NAV_FLOATING) all served correctly over HTTPS. Main chunk `assets/index-D8B6FauV.js` → 200 / 664,547 b / Content-Type `application/javascript; charset=utf-8`. Mobile iOS Safari UA returns byte-identical HTML to desktop (no UA-dependent CDN redirects). HTTPS headers clean (`Content-Type: text/html; charset=utf-8`, proper ETag, Vercel CDN caching active). Production URL is now the source of truth for the FB Instant Games ZIP — same code, same index.html, same `fbapp-config.json` in both.

## Stage 6 — Head-to-Head Migration
- **Decision: Daily Quiz is migrating from solo to Quiz Planet-style async head-to-head matches (2026-04-19).** Reason: solo has no virality, no retention beyond Day 1, cannot monetize on FB Audience Network without paid traffic. Multi-week migration. Stage 6.1 (this commit): FB context primitives (`chooseFriend`, `createMatchContext`, `getMatchContext`, `sendMatchUpdate`) added to platform adapters — facebook.js calls real `FBInstant.context.*` and `FBInstant.updateAsync` APIs, web.js returns mock contexts with `[web mock]` console prefix for dev visibility, telegram.js returns null/false stubs with `[TG stub] not implemented yet` logs. `src/game/match.js` state machine created (class Match with contextId, seed, questions=6 deterministic picks via `dateSeed ^ hashString(contextId)`, playerAScore/playerBScore, status pending_a→pending_b→complete, winnerId via Match.getWinner(), serialize/deserialize for storage, recordScore(playerId, score) state transitions). match.js is NOT imported from any existing code — solo gameplay loop is untouched and continues working on localhost and https://quiz-daily-omega.vercel.app. Next stages will wire the Match module into UI (match lobby, opponent picker, result-screen handoff) in separate commits.
- **Stage 6.3: "Play with Friend" button on Home + Match Lobby placeholder screen (2026-04-19).** Indigo/purple gradient button (linear-gradient 135° #4F46E5→#7C3AED, 220×80, same geometry as Daily PLAY) inserted on Home between the Daily Challenge block (PLAY button or "completed today" card) and the Quick Play section — visually distinct from emerald (solo primary) and amber (challenge banner). Click flow: `home.js` adds `.is-loading` class (opacity 0.6, cursor wait, pointer-events none) → awaits `callbacks.onPlayWithFriend()` → main.js `onPlayWithFriendClick()` calls `platform.chooseFriend()` → on cancel/null returns false and shows `t('matchCancelled')` toast (home.js removes loading state); on success lazy-imports Match + questions + matchStore, calls `Match.create(picked, questions)` + `saveMatch`, navigates to Match Lobby via `startMatchLobby(match)` which lazy-imports `src/game/matchLobby.js` and calls `renderMatchLobby(app, match, platform, () => showHome(true))`. Lobby shows: ← back arrow (top-left), `matchLobbyTitle`, opponent card (avatar circle with `match.opponent.photo` or 👤 fallback + `yourOpponent` caption + name), `matchInfo` line ("6 questions. You play first."), disabled emerald `startYourTurn` button with "Coming in next update" hint (Stage 6.4 will wire gameplay), secondary text-link `backToHome`. 8 new i18n keys added to EN/UA/NO: `playWithFriend`, `playWithFriendSub`, `matchCancelled`, `matchLobbyTitle`, `yourOpponent`, `matchInfo`, `startYourTurn`, `backToHome` (24 key occurrences total, grep-verified). Match class extended additively with `opponent` field (name+photo) — serialize/deserialize updated, state machine unchanged. Solo flow 100% preserved (main.js DOMContentLoaded + startGame untouched). Tree-shake intact: `matchDiag`/`__matchDiag`/`__diag__`/`window.__platform` all 0 matches in dist/assets. Bundle delta: Vite code-split more aggressively — main chunk 664.54→321.82 kB, new shared `theme-*.js` 346.91 kB (Tone.js hoisted because shared by main and matchLobby), total first-paint ~668 kB (flat vs Stage 6.2). New lazy chunk `matchLobby-*.js` 6.73 kB loads only on click.
- **Stage 6.2: match persistence layer + dev diag harness (2026-04-19).** `getContextData`/`setContextData` added to all three platform adapters — facebook.js wraps `FBInstant.player.getDataAsync(['match'])` / `setDataAsync({match: json})` (NB: FB rate-limits setDataAsync to ~1 write per 2s; wrapper is intentionally plain — callers must debounce), web.js uses localStorage key `mock_match_ctx` with JSON round-trip for dev testing, telegram.js stubs return null/false with `[TG stub]` logs. `src/game/matchStore.js` provides thin async helpers `saveMatch(match, platform)`, `loadMatch(platform)`, `clearMatch(platform)` — no caching or debouncing, direct platform bridge. `src/game/__diag__/matchDiag.js` exports `runMatchDiag(platform)` which creates a fresh Match, save→load→equality-check→clear→null-verify, returning `{ok: true}` or `{ok: false, error}`. Diag file attaches `window.__matchDiag` at import time. Diag is wired into main.js behind **two `import.meta.env.DEV` guards**: a top-of-file dynamic import (`if (import.meta.env.DEV) { import('./game/__diag__/matchDiag.js')... }`) and a post-platform-detect `window.__platform = platform` inside the DOMContentLoaded handler. Tree-shake verified: production build (`npm run build`) produces zero string matches for `matchDiag`, `__matchDiag`, `__diag__`, `matchStore`, `Match.create`, `MATCH_QUESTIONS`, `recordScore`, or `playerAScore` across `dist/assets/*.js`. Main chunk unchanged at 664.54 kB; facebook chunk grew 1.11→2.55 kB and web chunk 0.67→1.52 kB (only Stage 6.1/6.2 adapter methods, no diag leakage). Solo gameplay still fully functional; nothing new is UI-visible.

## Things we previously got wrong (and corrected)
- Pluto listed as smallest planet — Pluto was reclassified in 2006. Replaced distractor with Saturn.
- Sahara ambiguity — "largest desert" is Antarctica (ice). Changed question to "largest HOT desert", distractors adjusted.
- Questions file was declared multilingual in one prompt while the code still stored flat English strings — caused runtime `[object Object]` when screens tried to render. Lesson: structural changes to `questions.js` must migrate the Quiz class's `_localized()` helper in the same commit.
- Native `navigator.share` on Windows desktop shows the system picker (Outlook, Teams) — useless for viral sharing. Mobile-only gate added via `isMobile()` UA check; desktop always falls back to clipboard.
- Facebook share button was removed because FB reduces reach on posts that contain explicit share intents (the "share sheet" penalty). Telegram/WhatsApp/Viber + clipboard are the supported routes.
- Wordle is NOT our reference. Wordle is a web-native once-a-day puzzle. Our primary platform is Facebook Instant Games, where the monetization model is high session count × many sessions per day. Reference games going forward: AHA Quiz (16M MAU), WOW Quiz (8.3M), Nametests (6.8M). Every UX decision must be evaluated against "would AHA Quiz do this?"
- FB Instant Games re-engagement patterns that are mandatory for this project: (1) auto-continue countdown on result screen — "Next game in 3..2..1" with a big primary KEEP PLAYING button that's visually 2x larger than the Back button; (2) intra-session combo system with escalating labels (3 in a row = ON FIRE, 5 = UNSTOPPABLE, 7 = GOD MODE) and score multipliers; (3) progress bars everywhere ("You're X away from your best", "Top X% today"); (4) never demotivate — no copy like "does not count", "just practice", "unranked"; every alternative mode must have its OWN hook ("BEAT YOUR SCORE", "DAILY RECORD", "SPEED RUN").
- Question count per session is 12 (not 10). Do not hardcode the number anywhere in UI copy — always read from the config constant so future changes are one line.
- Auto-continue countdown on result screen was MY (the architect's) hallucination. Verified: Trivia Crack has no auto-continue (uses lives system that STOPS players). Hyper-casual games (Flappy Bird, Stack, Helix Jump) have manual tap-to-restart. No top game auto-starts next session. Removed.
- "Beat Your Score" mechanic has a GLASS CEILING (max = best score, then motivation dies). Replaced with LEVEL + XP (infinite progression like Trivia Crack's 240+ levels) and Coins (soft currency that depletes — will gate skins/boosts in Stage 6+).
- Question pool had 3 difficulty tiers but selection was random across them. Fixed: progressive curve now mandatory — easy first (warm-up), medium middle, hard finale. Matches HQ Trivia / Who Wants to be a Millionaire. Hard questions award 1.5× points so the finale feels cinematic.

### Session audit — April 2026 chat
Eight systemic mistakes the architect made and was caught on. Do not repeat:

1. **Used Wordle as reference game** — Wordle is a once-a-day web puzzle. Our primary platform is FB Instant Games. Correct references: AHA Quiz, WOW Quiz, Trivia Crack. Never cite Wordle again.
2. **Confused "FB reach algorithm" marketing rule with in-game Share button** — the algorithm rule applies to posts on FB pages linking to external sites. In-game Share through FBInstant.shareAsync is internal FB traffic.
3. **Invented "Beat your score" mechanic from memory** — has a GLASS CEILING (reaches max, motivation dies). Correct pattern is LEVEL + XP (infinite progression, Trivia Crack model) + soft currency (coins) that depletes.
4. **Invented auto-continue countdown on Result screen from memory** — no top game does this. Trivia Crack uses lives system that STOPS play. Hyper-casual games use manual tap-to-restart. Never auto-restart on behalf of the player.
5. **Proposed removing the Challenge-friends button as a "stub"** — this contradicts our own documented stance that Challenge-friends is the #1 viral driver (Trivia Crack's 200M organic downloads). Challenge and Share are DIFFERENT mechanics (Share = curiosity; Challenge = ego). Keep and enhance the button, never remove it.
6. **Added UI (XP pill, coin pill, progress bar, level-up overlay) without checking it fits the viewport** — result screen started overflowing on 768px-tall laptops. Every UI addition must verify min-height, safe-area insets, and scroll behavior in the same prompt.
7. **Claimed "all done" without verifying hardcoded copy was updated everywhere** — QUESTIONS_PER_SESSION = 12 but UI text in one language still said "10". Any constant-dependent copy must be grep-verified after every change.
8. **Wrote "AUDIT PASSED — no errors found" without actually auditing** — wrote it as a ritual header, not a real self-check. From now on, audit means listing the specific things checked, not a status emoji.

The root cause of all eight: writing from memory where research (web_search, conversation_search, grep) was required. The tool is faster than the memory and always more accurate than the architect's assumptions.

- Challenge-friends is the #1 viral engine for quiz games (Trivia Crack's 200M organic downloads). Architect wrongly proposed removing the "Виклик другу" button as a stub — Yurii pushed back. Stub is now functional foundation: `?challenge=X` URL param, personalized share message, golden incoming-challenge banner on Home screen. Stage 5 will upgrade FB flow to FBInstant.context.chooseAsync. Share triggers curiosity; Challenge triggers ego (3-5× stronger conversion). Never conflate them.
